#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([7. KONEKTOR ATX 24 PIN])
    Konektor ATX 24-pin adalah main power supply connector untuk motherboard.

    *Design:* Connector dibagi 2, satu power dari PSU, satu data paths untuk power delivery.]
})
