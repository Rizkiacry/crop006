#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([1. BIOS])
    BIOS (Basic Input/Output System) adalah firmware dalam chip berfungsi untuk hardware initialization, memverifikasi semua komponen sistem, dan mengload bootloader dari storage device.]
})
