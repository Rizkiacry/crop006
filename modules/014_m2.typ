#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([6. KONEKTOR M.2])
    M.2 adalah interface modern yang menggunakan PCIe lanes untuk komunikasi:

    *M.2 M Key:* Menggunakan PCIe x4 interface
    *M.2 B Key:* Menggunakan PCIe x2 atau SATA interface.
  ]
})
