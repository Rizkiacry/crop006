#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([10. VRM])
    VRM adalah step-down buck converter yang menyuplai CPU dan chipset. VRM terdiri dari power MOSFET, inductors, dan capacitors yang disolder pada motherboard.]
})
