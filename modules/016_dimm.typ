#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([8. SLOT DIMM])
    DIMM slots adalah konektor tempat RAM modules dipasang. 

    *DDR5 Memory:* Kecepatan hingga 8800 MT/s.

    *Memory Density:* 512 GB per DIMM.]
})
