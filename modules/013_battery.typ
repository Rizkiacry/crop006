#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([5. BATERAI])
    CMOS adalah baterai motherboard yang menyuplai daya ke real-time clock (RTC) dan CMOS memory saat PC mati. Terdapat quartz crystal oscillating pada 32.768 kHz. RTC chip menyimpan registers waktu dan mencegah revert ke default.]
})
