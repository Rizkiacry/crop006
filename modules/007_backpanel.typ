#import "../utils.typ": *
#import "../config.typ": *

#module-box((width, height) => {
  content-block(width, height)[
    #section-title([11. PANEL BELAKANG])
    Back panel adalah area external connectors terkumpul, dilengkapi dengan metal I/O shield untuk grounding
    
    *USB Ports* 
    *Audio Connectors*
    *Network Ethernet*
    *Display Outputs*
    *Grounding*]
})
